<h1>Job Listings</h1>

<div class="row">
  <div class="span8">
    <form class="form-search">
      <input type="text" class="input-medium search-query span6">
      <button type="submit" class="btn"><i class="icon-search"></i>Search</button>
    </form>

    <table class="jobs table table-hover">
      <thead>
        <tr>
          <th>Title & Categories</th>
          <th>Location</th>
        </tr>
      </thead>
      <tbody>
        <% @jobs.each do |job| %>
        <tr class="job-row" data-toggle="modal" 
            data-target="#job-modal-<%= job.id %>">
          <td class="job-title"><%= job.title %>
            <p class="job-categories"><%= job.description %></p>
          </td>
          <td class="job-location"><%= job.location %></td>
        </tr>
        <!-- Modal -->
        <div id="job-modal-<%= job.id %>" class="modal hide fade" 
             tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
             aria-hidden="true">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            <h3 id="myModalLabel"><%= job.title %></h3>
          </div>
          <div class="modal-body">
            <h4>Description</h4>
            <p>This is a description of the position and the sort of work you would be doing. You would read this but this is Hack Day so we should have just put a Nyan Cat in here because it would be prettier.</p>
            <h4>Responsibilities</h4>
            <ul>
              <li>Be on time.</li>
              <li>Be late sometimes to keep people on their toes.</li>
              <li>Support IE6</li>
              <li>Fluent in Visual Basic</li>
            </ul>
          </div>
          <div class="modal-footer">
            <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
            <% if user_signed_in? %>
              <button class="btn btn-success">Save for later</button>
              <button class="btn btn-primary">Apply</button>
            <% else %>
              <span>
                <%= link_to 'Sign up', new_user_registration_path %>
                or
                <%= link_to 'Sign in', new_user_session_path%>
                to Save or Apply for this job.
              </span>
            <% end %>
          </div>
        </div>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="span4">
    <!-- placeholder -->
    <h1 style="border: 1px solid #333333; width: 100%; height: 500px; text-align:center; line-height: 500px;">
      Map of USA
    </h1>
  </div>

  
</div>
